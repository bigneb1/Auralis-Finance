"use client";

import { useMemo, useState } from "react";
import { Button, Card, CardContent, CardHeader, CardTitle, StateWrapper, Switch } from "@auralis/ui";
const demoWallet = "0x8E21A2745f6F9aC4B8B9cA6e1F6Dd8a0A1C8E2D9";
const jurisdictions = ["NG", "EU", "US", "GB", "SG", "AE"];
const riskProfiles = ["Capital preservation", "Balanced treasury", "Yield-seeking"];
export default function SettingsPage() { const [jurisdiction, setJurisdiction] = useState("NG"); const [pendingJurisdiction, setPendingJurisdiction] = useState("NG"); const [riskProfile, setRiskProfile] = useState(riskProfiles[1]); const [mode, setMode] = useState("Advisory only"); const [appearance, setAppearance] = useState("System"); const [notifications, setNotifications] = useState({ rating: true, compliance: true, policy: false }); const [scanStatus, setScanStatus] = useState<"populated" | "loading" | "error" | "stale">("populated"); const [scanMessage, setScanMessage] = useState("No jurisdiction change pending."); const needsRescan = pendingJurisdiction !== jurisdiction; const profileSummary = useMemo(() => `${riskProfile} · ${mode} · ${appearance} appearance`, [riskProfile, mode, appearance]); async function applyJurisdiction() { setScanStatus("loading"); setScanMessage(`Re-scanning ${demoWallet} for ${pendingJurisdiction} jurisdiction…`); try { const res = await fetch("/api/compliance/scan", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ wallet: demoWallet, jurisdiction: pendingJurisdiction }) }); if (!res.ok) throw new Error(`Compliance scan failed: ${res.status}`); const report = await res.json() as { results?: { verdict: "ELIGIBLE" | "RESTRICTED" | "DENIED" }[]; reportHash?: string }; const results = report.results ?? []; const eligible = results.filter((item) => item.verdict === "ELIGIBLE").length; const restricted = results.filter((item) => item.verdict === "RESTRICTED").length; const denied = results.filter((item) => item.verdict === "DENIED").length; setJurisdiction(pendingJurisdiction); setScanStatus("populated"); setScanMessage(`Re-scan complete. ${eligible} eligible, ${restricted} restricted, ${denied} denied. Hash ${report.reportHash?.slice(0, 10)}…`); } catch (error) { setScanStatus("error"); setScanMessage(error instanceof Error ? error.message : "Compliance re-scan failed."); } } return <div className="space-y-5"><section><h1 className="font-display text-[32px] font-normal tracking-[-0.01em]">Settings</h1><p className="mt-1 max-w-3xl text-[var(--text-secondary)]">Profile, jurisdiction, risk profile, notifications, mode, appearance, and local safety controls.</p></section><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_360px]"><div className="space-y-4"><Setting title="Profile" action="Save"><div className="grid gap-4 sm:grid-cols-2"><Field label="Display name" defaultValue="Francis Treasury" /><Field label="Primary wallet" defaultValue={demoWallet} mono /></div><div className="mt-4 rounded-[10px] bg-[var(--surface-muted)] p-3 text-sm text-[var(--text-secondary)]">Current profile: {profileSummary}</div></Setting><Setting title="Jurisdiction declaration" action="Apply + re-scan" onAction={applyJurisdiction} disabled={!needsRescan || scanStatus === "loading"}><p className="mb-3 text-sm text-[var(--text-secondary)]">This feeds the compliance engine. Changing it prompts Auralis to re-scan asset eligibility before showing fresh verdicts.</p><select value={pendingJurisdiction} onChange={(event) => { setPendingJurisdiction(event.target.value); setScanStatus("stale"); setScanMessage("Jurisdiction changed. Re-scan required before verdicts should be trusted."); }} className="h-10 w-full rounded-[10px] border border-[var(--border)] bg-[var(--surface)] px-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--teal)]">{jurisdictions.map((item) => <option key={item} value={item}>{item}</option>)}</select><StateWrapper status={scanStatus}><p className="mt-4 rounded-[10px] border border-[var(--border)] bg-[var(--surface-muted)] p-3 text-sm text-[var(--text-secondary)]">{scanMessage}</p></StateWrapper></Setting><Setting title="Risk profile" action="Save"><div className="grid gap-3 sm:grid-cols-3">{riskProfiles.map((profile) => <button key={profile} onClick={() => setRiskProfile(profile)} className={`rounded-[10px] border p-4 text-left focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--teal)] ${riskProfile === profile ? "border-[var(--teal)] bg-[var(--teal-wash)]" : "border-[var(--border)] bg-[var(--surface-muted)] hover:bg-[var(--teal-wash)]"}`}><span className="font-medium">{profile}</span><span className="mt-1 block text-xs text-[var(--text-secondary)]">Adjusts policy defaults and copilot explanations.</span></button>)}</div></Setting><Setting title="Notifications" action="Save"><div className="grid gap-3">{Object.entries(notifications).map(([key, value]) => <label key={key} className="flex items-center justify-between rounded-[10px] border border-[var(--border)] bg-[var(--surface-muted)] p-3"><span className="capitalize">{key} alerts</span><Switch checked={value} onChange={(event) => setNotifications((current) => ({ ...current, [key]: event.target.checked }))} /></label>)}</div></Setting></div><aside className="space-y-4"><Setting title="Mode" action="Save"><select value={mode} onChange={(event) => setMode(event.target.value)} className="h-10 w-full rounded-[10px] border border-[var(--border)] bg-[var(--surface)] px-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--teal)]"><option>Advisory only</option><option>Guarded execution preview</option><option>Read-only judge demo</option></select><p className="mt-2 text-sm text-[var(--text-secondary)]">Auralis never signs transactions. Execution remains user-signed.</p></Setting><Setting title="Appearance" action="Save"><select value={appearance} onChange={(event) => setAppearance(event.target.value)} className="h-10 w-full rounded-[10px] border border-[var(--border)] bg-[var(--surface)] px-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--teal)]"><option>System</option><option>Light</option><option>High contrast</option></select></Setting><Card className="border-[var(--rose)]/40"><CardHeader><div className="text-xs font-semibold uppercase tracking-wide text-[var(--rose)]">Danger zone</div><CardTitle>Local reset controls</CardTitle></CardHeader><CardContent className="space-y-4 text-sm text-[var(--text-secondary)]"><p>Clears local preferences only. It does not revoke attestations or touch on-chain records.</p><Button variant="secondary" onClick={() => { setPendingJurisdiction("NG"); setJurisdiction("NG"); setRiskProfile(riskProfiles[1]); setMode("Advisory only"); setAppearance("System"); setScanMessage("Local settings reset. No on-chain action performed."); }}>Reset local demo settings</Button></CardContent></Card></aside></div></div>; }
function Setting({title,action,onAction,disabled,children}:{title:string;action:string;onAction?:()=>void;disabled?:boolean;children:React.ReactNode}){return <Card><CardHeader><div className="flex items-center justify-between gap-3"><CardTitle>{title}</CardTitle><Button size="sm" variant="secondary" onClick={onAction} disabled={disabled}>{action}</Button></div></CardHeader><CardContent>{children}</CardContent></Card>}
function Field({ label, defaultValue, mono }: { label: string; defaultValue: string; mono?: boolean }) { return <label className="grid gap-1 text-sm"><span className="text-[var(--text-secondary)]">{label}</span><input defaultValue={defaultValue} className={`h-10 rounded-[10px] border border-[var(--border)] bg-[var(--surface)] px-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--teal)] ${mono?'font-mono':''}`} /></label>; }
